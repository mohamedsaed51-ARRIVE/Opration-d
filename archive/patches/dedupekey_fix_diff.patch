diff --git a/index.html b/index.html
index ac27eda..e155036 100644
--- a/index.html
+++ b/index.html
@@ -1770,10 +1770,22 @@ function fetchMonthDashboardSummary(month, opts){
   opts = opts || {};
   const filtersKey = opts.filters ? JSON.stringify(opts.filters) : '';
   const slaTargetsKey = opts.branchSlaTargets ? JSON.stringify(opts.branchSlaTargets) : '';
+  // BUG FIX: forceRefresh determines whether refresh=1 is actually sent to
+  // the backend (see below) — which in turn decides whether Code.gs's
+  // allowHeavyLiveFallback can ever be true. It was missing from this key,
+  // so a normal request already in flight for the same month/filters/SLA
+  // could silently absorb a later Manual Refresh into the SAME promise
+  // (never actually sending refresh=1), or the reverse — a normal/
+  // background request could inherit an in-flight Manual Refresh's
+  // refresh=1. Both directions defeat the earlier force/background
+  // separation fix. '|refresh:1' vs '|refresh:0' keeps the two dedupe
+  // buckets distinct while still letting two truly identical requests
+  // (same forceRefresh value, same everything else) share one promise.
   const dedupeKey = 'dashboard:'+month+(opts.branch ? ('|b:'+opts.branch) : '')
     +(filtersKey?('|f:'+filtersKey):'')+(opts.dateFrom?('|df:'+opts.dateFrom):'')+(opts.dateTo?('|dt:'+opts.dateTo):'')
     +(opts.attemptT1!=null?('|t1:'+opts.attemptT1):'')+(opts.attemptT2!=null?('|t2:'+opts.attemptT2):'')
-    +(opts.slaTarget!=null?('|sla:'+opts.slaTarget):'')+(slaTargetsKey?('|slaov:'+slaTargetsKey):'');
+    +(opts.slaTarget!=null?('|sla:'+opts.slaTarget):'')+(slaTargetsKey?('|slaov:'+slaTargetsKey):'')
+    +(opts.forceRefresh?'|refresh:1':'|refresh:0');
   return dedupeFetch(dedupeKey, async ()=>{
     const params = { action:'dashboard', sheet: month };
     if(opts.forceRefresh) params.refresh = '1';
